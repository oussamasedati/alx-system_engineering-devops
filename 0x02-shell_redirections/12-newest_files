#!/bin/bash

# Use the ls command to list all files in the current directory, sorted by modification time in reverse order
# using the -t and -r options, and display the first 10 lines using the head command
ls -tr | tail -n 10

