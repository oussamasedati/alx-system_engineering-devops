#!/bin/bash

# Use the find command to locate all the regular files (not directories) with a .js extension
# in the current directory and its subfolders, and delete them using the rm command
find . -type f -name "*.js" -delete

